version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting pre-build phase"
    build:
      commands:
        - echo "Starting build phase"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
